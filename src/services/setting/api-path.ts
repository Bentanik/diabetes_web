/* eslint-disable import/no-anonymous-default-export */
const SETTING = "http://localhost:5023/api/v1/settings";

const SETTING_CHAT = `${SETTING}/chat`;

export default {
  SETTING,
  SETTING_CHAT,
};
